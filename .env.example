# ===========================================
# SanyuTech DX Platform - 環境変数設定
# ===========================================
# このファイルをコピーして .env を作成してください
# cp .env.example .env

# ----- バージョン -----
RUBY_VERSION=3.3.0
NODE_VERSION=20
RAILS_VERSION=8.0.1
POSTGRES_VERSION=16-alpine
N8N_VERSION=2.1.1

# ----- PostgreSQL -----
POSTGRES_USER=sanyu
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=sanyu_development
POSTGRES_PORT=5432

# ----- アプリケーションポート -----
PLATFORM_PORT=3001
WORKER_WEB_PORT=3002
N8N_PORT=5678

# ----- Rails -----
RAILS_ENV=development
RAILS_LOG_TO_STDOUT=true

# ----- タイムゾーン -----
TZ=Asia/Tokyo

# ----- 本番環境設定 -----
# 許可するホスト名（VPS公開時に設定）
# ALLOWED_HOST=your-domain.com

# ----- バックアップ設定 -----
# 通知用Webhook URL（任意）
# BACKUP_WEBHOOK_URL=https://your-webhook-url

# バックアップ暗号化（true/false）
# 有効にする場合はBACKUP_ENCRYPTION_KEYも設定必須
# キー生成コマンド: openssl rand -base64 32
ENCRYPT_BACKUP=false
# BACKUP_ENCRYPTION_KEY=ここに生成したキーを貼る

# Google Drive同期（true/false）
# 事前にrclone configでgdriveリモートの設定が必要
GDRIVE_SYNC=false
# GDRIVE_BACKUP_FOLDER=SanyuTech_Backups

# バックアップ保持日数
RETENTION_DAYS=7
