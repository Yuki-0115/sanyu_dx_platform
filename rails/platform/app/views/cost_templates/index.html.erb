<div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
  <div class="mb-6">
    <h1 class="text-2xl font-bold text-gray-900">単価テンプレート</h1>
    <p class="text-sm text-gray-500 mt-1">案件ごとに材料・外注・機械の単価を登録し、職長が日報入力時に参照できます</p>
  </div>

  <% if @projects_with_templates.any? %>
    <div class="mb-8">
      <h2 class="text-lg font-semibold text-gray-800 mb-4">登録済み案件</h2>
      <div class="bg-white rounded-lg shadow overflow-hidden">
        <table class="w-full">
          <thead class="bg-gray-50">
            <tr>
              <th class="px-4 py-3 text-left text-sm font-medium text-gray-500">案件コード</th>
              <th class="px-4 py-3 text-left text-sm font-medium text-gray-500">案件名</th>
              <th class="px-4 py-3 text-center text-sm font-medium text-gray-500">登録数</th>
              <th class="px-4 py-3 text-center text-sm font-medium text-gray-500">カテゴリ</th>
              <th class="px-4 py-3 text-center text-sm font-medium text-gray-500">操作</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-gray-200">
            <% @projects_with_templates.each do |project| %>
              <% templates = project.project_cost_templates %>
              <% categories = templates.map(&:category).uniq %>
              <tr class="hover:bg-gray-50">
                <td class="px-4 py-3 text-sm font-mono text-gray-600"><%= project.code %></td>
                <td class="px-4 py-3 text-sm font-medium">
                  <%= link_to project.name, project_path(project), class: "text-blue-600 hover:underline" %>
                </td>
                <td class="px-4 py-3 text-center">
                  <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                    <%= templates.count %>件
                  </span>
                </td>
                <td class="px-4 py-3 text-center">
                  <div class="flex flex-wrap justify-center gap-1">
                    <% categories.each do |cat| %>
                      <span class="px-2 py-0.5 text-xs rounded bg-gray-100 text-gray-600">
                        <%= ProjectCostTemplate::CATEGORY_LABELS[cat] %>
                      </span>
                    <% end %>
                  </div>
                </td>
                <td class="px-4 py-3 text-center">
                  <%= link_to "管理", project_project_cost_templates_path(project),
                      class: "px-3 py-1 text-sm bg-blue-600 text-white rounded hover:bg-blue-700" %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  <% end %>

  <% if @projects_without_templates.any? %>
    <div>
      <h2 class="text-lg font-semibold text-gray-800 mb-4">未登録案件</h2>
      <div class="bg-white rounded-lg shadow overflow-hidden">
        <table class="w-full">
          <thead class="bg-gray-50">
            <tr>
              <th class="px-4 py-3 text-left text-sm font-medium text-gray-500">案件コード</th>
              <th class="px-4 py-3 text-left text-sm font-medium text-gray-500">案件名</th>
              <th class="px-4 py-3 text-left text-sm font-medium text-gray-500">ステータス</th>
              <th class="px-4 py-3 text-center text-sm font-medium text-gray-500">操作</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-gray-200">
            <% @projects_without_templates.each do |project| %>
              <tr class="hover:bg-gray-50">
                <td class="px-4 py-3 text-sm font-mono text-gray-600"><%= project.code %></td>
                <td class="px-4 py-3 text-sm">
                  <%= link_to project.name, project_path(project), class: "text-blue-600 hover:underline" %>
                </td>
                <td class="px-4 py-3 text-sm">
                  <span class="px-2 py-1 text-xs rounded-full <%= project.status == 'in_progress' ? 'bg-blue-100 text-blue-800' : 'bg-gray-100 text-gray-800' %>">
                    <%= Project::STATUS_LABELS[project.status] rescue project.status %>
                  </span>
                </td>
                <td class="px-4 py-3 text-center">
                  <%= link_to "登録", new_project_project_cost_template_path(project),
                      class: "px-3 py-1 text-sm border border-blue-600 text-blue-600 rounded hover:bg-blue-50" %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  <% end %>

  <% if @projects.empty? %>
    <div class="bg-white rounded-lg shadow p-8 text-center text-gray-500">
      <p>対象となる案件がありません</p>
      <p class="text-sm mt-2">受注済み・着工準備中・施工中の案件が表示されます</p>
    </div>
  <% end %>
</div>
