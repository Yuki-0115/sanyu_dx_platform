<div class="min-h-screen bg-gray-100">
  <%= render "shared/navbar" %>

  <main class="max-w-3xl mx-auto py-6 sm:px-6 lg:px-8">
    <div class="px-4 py-6 sm:px-0">
      <div class="mb-6">
        <h2 class="text-2xl font-bold text-gray-900">新規日報作成</h2>
        <p class="text-sm text-gray-500 mt-1">日報の種類を選択してください</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <!-- 案件日報 -->
        <div class="bg-white shadow rounded-lg p-6">
          <div class="flex items-center mb-4">
            <div class="flex-shrink-0 h-10 w-10 rounded-full bg-blue-100 flex items-center justify-center">
              <svg class="h-6 w-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/>
              </svg>
            </div>
            <div class="ml-4">
              <h3 class="text-lg font-medium text-gray-900">案件日報</h3>
              <p class="text-sm text-gray-500">登録済みの案件に対する日報</p>
            </div>
          </div>

          <% if @projects.any? %>
            <div class="mt-4">
              <label class="block text-sm font-medium text-gray-700 mb-2">案件を選択</label>
              <div class="space-y-2 max-h-64 overflow-y-auto">
                <% @projects.each do |project| %>
                  <%= link_to new_project_daily_report_path(project),
                      class: "block p-3 border border-gray-200 rounded-lg hover:bg-blue-50 hover:border-blue-300 transition-colors" do %>
                    <div class="flex justify-between items-center">
                      <div>
                        <div class="font-medium text-gray-900"><%= project.name %></div>
                        <div class="text-xs text-gray-500"><%= project.code %> / <%= project.client&.name %></div>
                      </div>
                      <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                      </svg>
                    </div>
                  <% end %>
                <% end %>
              </div>
            </div>
          <% else %>
            <div class="mt-4 p-4 bg-gray-50 rounded-lg text-center">
              <p class="text-sm text-gray-500">進行中の案件がありません</p>
              <%= link_to "案件を作成", new_project_path, class: "mt-2 inline-block text-blue-600 hover:underline text-sm" %>
            </div>
          <% end %>
        </div>

        <!-- 常用日報 -->
        <div class="bg-white shadow rounded-lg p-6">
          <div class="flex items-center mb-4">
            <div class="flex-shrink-0 h-10 w-10 rounded-full bg-orange-100 flex items-center justify-center">
              <svg class="h-6 w-6 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
              </svg>
            </div>
            <div class="ml-4">
              <h3 class="text-lg font-medium text-gray-900">常用日報</h3>
              <p class="text-sm text-gray-500">外部現場での常用作業の日報</p>
            </div>
          </div>

          <div class="mt-4 p-4 bg-orange-50 rounded-lg">
            <p class="text-sm text-orange-700 mb-4">
              案件に紐づかない外部現場での作業を報告する場合に使用します。
            </p>
            <%= link_to new_external_daily_report_path,
                class: "w-full inline-flex justify-center items-center px-4 py-3 border border-transparent text-sm font-medium rounded-md text-white bg-orange-600 hover:bg-orange-700" do %>
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
              </svg>
              常用日報を作成
            <% end %>
          </div>
        </div>
      </div>

      <div class="mt-6 text-center">
        <%= link_to "戻る", all_daily_reports_path, class: "text-gray-600 hover:underline" %>
      </div>
    </div>
  </main>
</div>
