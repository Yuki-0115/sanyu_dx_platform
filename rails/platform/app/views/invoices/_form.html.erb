<%= form_with model: [@project, invoice], class: "space-y-6" do |f| %>
  <% if invoice.errors.any? %>
    <div class="rounded-md bg-red-50 p-4">
      <div class="flex">
        <div class="flex-shrink-0">
          <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
          </svg>
        </div>
        <div class="ml-3">
          <h3 class="text-sm font-medium text-red-800">入力エラーがあります</h3>
          <ul class="mt-2 list-disc list-inside text-sm text-red-700">
            <% invoice.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  <% end %>

  <!-- 基本情報 -->
  <div class="bg-white shadow rounded-lg p-6">
    <h3 class="text-lg font-medium text-gray-900 mb-4">基本情報</h3>

    <div class="grid grid-cols-1 sm:grid-cols-3 gap-6">
      <div>
        <%= f.label :invoice_number, "請求番号", class: "block text-sm font-medium text-gray-700" %>
        <%= f.text_field :invoice_number, placeholder: "例: INV-2026-001",
            class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
      </div>

      <div>
        <%= f.label :issued_date, "請求日", class: "block text-sm font-medium text-gray-700" %>
        <%= f.date_field :issued_date,
            class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
      </div>

      <div>
        <%= f.label :due_date, "支払期限", class: "block text-sm font-medium text-gray-700" %>
        <%= f.date_field :due_date,
            class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
      </div>
    </div>
  </div>

  <!-- 金額 -->
  <div class="bg-white shadow rounded-lg p-6">
    <h3 class="text-lg font-medium text-gray-900 mb-4">金額</h3>

    <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
      <div>
        <%= f.label :amount, "税抜金額 (円)", class: "block text-sm font-medium text-gray-700" %>
        <%= f.number_field :amount, step: 1, placeholder: "0", required: true,
            class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
      </div>

      <div>
        <%= f.label :tax_amount, "消費税 (円)", class: "block text-sm font-medium text-gray-700" %>
        <%= f.number_field :tax_amount, step: 1, placeholder: "0",
            class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
        <p class="mt-1 text-xs text-gray-500">総額は税抜金額 + 消費税で自動計算</p>
      </div>
    </div>

    <% if @project.order_amount %>
      <div class="mt-4 p-3 bg-gray-50 rounded-lg">
        <p class="text-sm text-gray-600">
          受注金額: <%= number_to_currency(@project.order_amount, unit: "¥", precision: 0) %>
        </p>
      </div>
    <% end %>
  </div>

  <!-- 備考 -->
  <div class="bg-white shadow rounded-lg p-6">
    <%= f.label :description, "備考", class: "block text-sm font-medium text-gray-700 mb-2" %>
    <%= f.text_area :description, rows: 4, placeholder: "請求内容の詳細など",
        class: "block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
  </div>

  <div class="flex justify-end space-x-3">
    <%= link_to "キャンセル", invoice.persisted? ? project_invoice_path(@project, invoice) : project_invoices_path(@project),
        class: "inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50" %>
    <%= f.submit invoice.persisted? ? "更新" : "作成",
        class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 cursor-pointer" %>
  </div>
<% end %>
