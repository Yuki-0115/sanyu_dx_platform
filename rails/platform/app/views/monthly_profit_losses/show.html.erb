<div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
  <%# ヘッダー %>
  <div class="flex justify-between items-center mb-6">
    <div>
      <h1 class="text-2xl font-bold text-gray-900"><%= @year %>年<%= @month %>月 月次損益</h1>
      <p class="text-sm text-gray-500">売上・原価・粗利の詳細</p>
    </div>
    <div class="flex space-x-2">
      <% prev_date = Date.new(@year, @month, 1).prev_month %>
      <% next_date = Date.new(@year, @month, 1).next_month %>
      <%= link_to monthly_profit_loss_path(year: prev_date.year, month: prev_date.month),
          class: "inline-flex items-center px-3 py-2 border border-gray-300 rounded-md text-sm font-medium text-gray-700 bg-white hover:bg-gray-50" do %>
        <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
        </svg>
        <%= prev_date.month %>月
      <% end %>
      <%= link_to monthly_profit_loss_path(year: next_date.year, month: next_date.month),
          class: "inline-flex items-center px-3 py-2 border border-gray-300 rounded-md text-sm font-medium text-gray-700 bg-white hover:bg-gray-50" do %>
        <%= next_date.month %>月
        <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
        </svg>
      <% end %>
      <%= link_to "一覧", monthly_profit_losses_path,
          class: "inline-flex items-center px-3 py-2 border border-gray-300 rounded-md text-sm font-medium text-gray-700 bg-white hover:bg-gray-50" %>
      <%= link_to "月次損益計算書", monthly_income_statement_path(year: @year, month: @month),
          class: "inline-flex items-center px-3 py-2 border border-indigo-300 rounded-md text-sm font-medium text-indigo-700 bg-indigo-50 hover:bg-indigo-100" %>
    </div>
  </div>

  <%# サマリーカード（出来高ベース：経営指標） %>
  <div class="mb-2">
    <h2 class="text-sm font-medium text-gray-500 flex items-center">
      <span class="inline-block w-3 h-3 bg-blue-500 rounded-full mr-2"></span>
      出来高ベース（経営指標）
    </h2>
  </div>
  <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-6">
    <div class="bg-blue-50 rounded-lg shadow p-4">
      <dt class="text-sm font-medium text-blue-700">月次出来高</dt>
      <dd class="mt-1 text-2xl font-bold text-blue-900">
        <%= number_to_currency(@progress_revenue_total, unit: "¥", precision: 0) %>
      </dd>
    </div>
    <div class="bg-orange-50 rounded-lg shadow p-4">
      <dt class="text-sm font-medium text-orange-700">原価</dt>
      <dd class="mt-1 text-2xl font-bold text-orange-900">
        <%= number_to_currency(@total_cost, unit: "¥", precision: 0) %>
      </dd>
    </div>
    <div class="bg-white rounded-lg shadow p-4 border-2 <%= @progress_gross_profit >= 0 ? 'border-green-500' : 'border-red-500' %>">
      <dt class="text-sm font-medium text-gray-700">粗利</dt>
      <dd class="mt-1 text-2xl font-bold <%= @progress_gross_profit >= 0 ? 'text-green-600' : 'text-red-600' %>">
        <%= number_to_currency(@progress_gross_profit, unit: "¥", precision: 0) %>
      </dd>
    </div>
    <div class="bg-white rounded-lg shadow p-4">
      <dt class="text-sm font-medium text-gray-700">利益率</dt>
      <dd class="mt-1 text-2xl font-bold <%= @progress_profit_rate >= 0 ? 'text-green-600' : 'text-red-600' %>">
        <%= @progress_profit_rate %>%
      </dd>
    </div>
  </div>

  <%# サマリーカード（請求ベース：会計確定） %>
  <div class="mb-2">
    <h2 class="text-sm font-medium text-gray-500 flex items-center">
      <span class="inline-block w-3 h-3 bg-green-500 rounded-full mr-2"></span>
      請求ベース（会計確定）
    </h2>
  </div>
  <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-6">
    <div class="bg-green-50 rounded-lg shadow p-4">
      <dt class="text-sm font-medium text-green-700">月次請求額</dt>
      <dd class="mt-1 text-2xl font-bold text-green-900">
        <%= number_to_currency(@invoiced_revenue, unit: "¥", precision: 0) %>
      </dd>
    </div>
    <div class="bg-orange-50 rounded-lg shadow p-4">
      <dt class="text-sm font-medium text-orange-700">原価</dt>
      <dd class="mt-1 text-2xl font-bold text-orange-900">
        <%= number_to_currency(@total_cost, unit: "¥", precision: 0) %>
      </dd>
    </div>
    <div class="bg-white rounded-lg shadow p-4 border-2 <%= @invoiced_gross_profit >= 0 ? 'border-green-500' : 'border-red-500' %>">
      <dt class="text-sm font-medium text-gray-700">粗利</dt>
      <dd class="mt-1 text-2xl font-bold <%= @invoiced_gross_profit >= 0 ? 'text-green-600' : 'text-red-600' %>">
        <%= number_to_currency(@invoiced_gross_profit, unit: "¥", precision: 0) %>
      </dd>
    </div>
    <div class="bg-white rounded-lg shadow p-4">
      <dt class="text-sm font-medium text-gray-700">利益率</dt>
      <dd class="mt-1 text-2xl font-bold <%= @invoiced_profit_rate >= 0 ? 'text-green-600' : 'text-red-600' %>">
        <%= @invoiced_profit_rate %>%
      </dd>
    </div>
  </div>

  <%# 整合性チェック（出来高 = 請求 + 仕掛かり） %>
  <div class="bg-gray-50 border border-gray-200 rounded-lg p-4 mb-6">
    <div class="flex items-center justify-between text-sm">
      <span class="text-gray-600">出来高 = 請求 + 仕掛かり</span>
      <span class="font-medium text-gray-900">
        <%= number_to_currency(@progress_revenue_total, unit: "¥", precision: 0) %> =
        <%= number_to_currency(@invoiced_revenue, unit: "¥", precision: 0) %> +
        <%= number_to_currency(@wip_revenue_total, unit: "¥", precision: 0) %>
      </span>
    </div>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
    <%# 左側: 売上詳細 %>
    <div class="space-y-4">
      <%# 月次出来高（案件別） %>
      <div class="bg-white shadow rounded-lg overflow-hidden border-l-4 border-blue-400">
        <div class="px-4 py-3 border-b border-gray-200 bg-blue-50">
          <div class="flex justify-between items-center">
            <h3 class="text-lg font-medium text-blue-900">月次出来高</h3>
            <span class="text-lg font-bold text-blue-900"><%= number_to_currency(@progress_revenue_total, unit: "¥", precision: 0) %></span>
          </div>
          <p class="text-xs text-blue-600 mt-1">今月の出来高（案件別）</p>
        </div>
        <% if @monthly_progresses.any? %>
          <div class="divide-y divide-gray-100">
            <% @monthly_progresses.values.sort_by { |p| -p.monthly_progress_amount.to_i }.each do |progress| %>
              <% monthly_amount = progress.monthly_progress_amount %>
              <% next if monthly_amount <= 0 %>
              <div class="px-4 py-3 hover:bg-blue-50">
                <div class="flex justify-between items-start">
                  <div>
                    <%= link_to progress.project.name, project_path(progress.project), class: "text-sm font-medium text-gray-900 hover:text-blue-600" %>
                    <% if progress.note.present? %>
                      <p class="text-xs text-gray-500"><%= progress.note %></p>
                    <% end %>
                    <p class="text-xs text-gray-400">累計: <%= number_to_currency(progress.progress_amount, unit: "¥", precision: 0) %></p>
                  </div>
                  <span class="text-sm font-medium text-blue-900"><%= number_to_currency(monthly_amount, unit: "¥", precision: 0) %></span>
                </div>
              </div>
            <% end %>
          </div>
        <% else %>
          <div class="p-4 text-center text-gray-500 text-sm">
            今月の出来高はありません（各案件で入力）
          </div>
        <% end %>
      </div>

      <%# 請求済み売上 %>
      <div class="bg-white shadow rounded-lg overflow-hidden border-l-4 border-green-400">
        <div class="px-4 py-3 border-b border-gray-200 bg-green-50">
          <div class="flex justify-between items-center">
            <h3 class="text-lg font-medium text-green-900">当月請求額</h3>
            <span class="text-lg font-bold text-green-900"><%= number_to_currency(@invoiced_revenue, unit: "¥", precision: 0) %></span>
          </div>
          <p class="text-xs text-green-600 mt-1">今月出来高に対する請求</p>
        </div>
        <% if @invoiced_projects.any? %>
          <div class="divide-y divide-gray-100">
            <% @invoiced_projects.each do |ip| %>
              <div class="px-4 py-3 hover:bg-green-50">
                <div class="flex justify-between items-start">
                  <div>
                    <%= link_to ip[:project].name, project_path(ip[:project]), class: "text-sm font-medium text-gray-900 hover:text-blue-600" %>
                    <p class="text-xs text-gray-500">
                      <%= ip[:invoice_number] %>
                      <% if ip[:issued_date] %>
                        <span class="ml-1 text-gray-400">発行: <%= ip[:issued_date].strftime("%m/%d") %></span>
                      <% end %>
                    </p>
                  </div>
                  <span class="text-sm font-medium text-green-900"><%= number_to_currency(ip[:amount], unit: "¥", precision: 0) %></span>
                </div>
              </div>
            <% end %>
          </div>
        <% else %>
          <div class="p-4 text-center text-gray-500 text-sm">
            今月出来高への請求はありません
          </div>
        <% end %>
      </div>

      <%# 仕掛かり（当月出来高 - 当月請求） %>
      <% if @wip_revenue_total > 0 %>
        <div class="bg-white shadow rounded-lg overflow-hidden border-l-4 border-yellow-400">
          <div class="px-4 py-3 border-b border-gray-200 bg-yellow-50">
            <div class="flex justify-between items-center">
              <h3 class="text-lg font-medium text-yellow-900">当月仕掛かり</h3>
              <span class="text-lg font-bold text-yellow-900"><%= number_to_currency(@wip_revenue_total, unit: "¥", precision: 0) %></span>
            </div>
            <p class="text-xs text-yellow-600 mt-1">当月出来高 - 当月請求</p>
          </div>
          <% if @wip_projects_data.any? %>
            <div class="divide-y divide-gray-100">
              <% @wip_projects_data.each do |wp| %>
                <div class="px-4 py-3 hover:bg-yellow-50">
                  <div class="flex justify-between items-start">
                    <div>
                      <%= link_to wp[:project].name, project_path(wp[:project]), class: "text-sm font-medium text-gray-700 hover:text-blue-600" %>
                      <p class="text-xs text-gray-500">
                        出来高: <%= number_to_currency(wp[:monthly_progress], unit: "¥", precision: 0) %> -
                        請求: <%= number_to_currency(wp[:invoiced], unit: "¥", precision: 0) %>
                      </p>
                    </div>
                    <span class="text-sm font-bold text-yellow-800"><%= number_to_currency(wp[:wip_amount], unit: "¥", precision: 0) %></span>
                  </div>
                </div>
              <% end %>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>

    <%# 右側: 原価 %>
    <div class="space-y-4">
      <%# 労務費 %>
      <details class="bg-white shadow rounded-lg overflow-hidden group">
        <summary class="px-4 py-3 border-b border-gray-200 bg-indigo-50 cursor-pointer list-none">
          <div class="flex justify-between items-center">
            <div class="flex items-center">
              <svg class="w-4 h-4 mr-2 text-indigo-600 transform group-open:rotate-90 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
              </svg>
              <h3 class="text-base font-medium text-indigo-900">労務費</h3>
              <% if @salary_confirmed %>
                <span class="ml-2 px-2 py-0.5 text-xs bg-green-100 text-green-800 rounded-full">確定</span>
              <% else %>
                <span class="ml-2 px-2 py-0.5 text-xs bg-yellow-100 text-yellow-800 rounded-full">概算</span>
              <% end %>
            </div>
            <span class="text-base font-bold text-indigo-900"><%= number_to_currency(@total_labor_cost, unit: "¥", precision: 0) %></span>
          </div>
        </summary>
        <div class="divide-y divide-gray-100">
          <% @labor_details.each do |detail| %>
            <div class="px-4 py-2 flex justify-between text-sm">
              <span class="text-gray-700"><%= detail[:name] %></span>
              <span class="text-gray-900"><%= number_to_currency(detail[:amount], unit: "¥", precision: 0) %></span>
            </div>
          <% end %>
          <% if @labor_details.empty? %>
            <div class="px-4 py-3 text-sm text-gray-500">データなし</div>
          <% end %>
          <% if !@salary_confirmed %>
            <div class="px-4 py-2 bg-yellow-50">
              <%= link_to "確定給与を入力", monthly_salaries_path(year: @year, month: @month),
                  class: "text-sm text-yellow-700 hover:underline" %>
            </div>
          <% end %>
        </div>
      </details>

      <%# 外注費 %>
      <details class="bg-white shadow rounded-lg overflow-hidden group">
        <summary class="px-4 py-3 border-b border-gray-200 bg-orange-50 cursor-pointer list-none">
          <div class="flex justify-between items-center">
            <div class="flex items-center">
              <svg class="w-4 h-4 mr-2 text-orange-600 transform group-open:rotate-90 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
              </svg>
              <h3 class="text-base font-medium text-orange-900">外注費</h3>
              <% if @outsourcing_confirmed %>
                <span class="ml-2 px-2 py-0.5 text-xs bg-green-100 text-green-800 rounded-full">確定</span>
              <% else %>
                <span class="ml-2 px-2 py-0.5 text-xs bg-yellow-100 text-yellow-800 rounded-full">概算</span>
              <% end %>
            </div>
            <span class="text-base font-bold text-orange-900"><%= number_to_currency(@total_outsourcing_cost, unit: "¥", precision: 0) %></span>
          </div>
        </summary>
        <div class="divide-y divide-gray-100">
          <% if @outsourcing_confirmed %>
            <%# 確定時は協力会社別合計を表示 %>
            <% @confirmed_outsourcing_by_partner.each do |partner_name, amount| %>
              <div class="px-4 py-2 flex justify-between text-sm">
                <span class="text-gray-700"><%= partner_name %></span>
                <span class="text-gray-900"><%= number_to_currency(amount, unit: "¥", precision: 0) %></span>
              </div>
            <% end %>
            <% if @outsourcing_cost_difference && @outsourcing_cost_difference != 0 %>
              <div class="px-4 py-2 bg-gray-50 flex justify-between text-sm">
                <span class="text-gray-500">概算との差額</span>
                <span class="<%= @outsourcing_cost_difference > 0 ? 'text-red-600' : 'text-green-600' %>">
                  <%= @outsourcing_cost_difference > 0 ? '+' : '' %><%= number_to_currency(@outsourcing_cost_difference, unit: "¥", precision: 0) %>
                </span>
              </div>
            <% end %>
          <% else %>
            <%# 概算時は日報ベースの詳細を表示 %>
            <% @outsourcing_details.each do |detail| %>
              <div class="px-4 py-2 flex justify-between text-sm">
                <div>
                  <span class="text-gray-700"><%= detail[:name] %></span>
                  <% if detail[:man_days] > 0 && detail[:contract] > 0 %>
                    <span class="text-xs text-gray-500 ml-2">(人工: <%= number_to_currency(detail[:man_days], unit: "", precision: 0) %> / 請負: <%= number_to_currency(detail[:contract], unit: "", precision: 0) %>)</span>
                  <% end %>
                </div>
                <span class="text-gray-900"><%= number_to_currency(detail[:total], unit: "¥", precision: 0) %></span>
              </div>
            <% end %>
            <% if @outsourcing_details.empty? %>
              <div class="px-4 py-3 text-sm text-gray-500">データなし</div>
            <% end %>
          <% end %>
          <div class="px-4 py-2 bg-orange-50">
            <%= link_to "確定外注費を入力", monthly_outsourcing_costs_path(year: @year, month: @month),
                class: "text-sm text-orange-700 hover:underline" %>
          </div>
        </div>
      </details>

      <%# 材料費 %>
      <details class="bg-white shadow rounded-lg overflow-hidden group">
        <summary class="px-4 py-3 border-b border-gray-200 bg-green-50 cursor-pointer list-none">
          <div class="flex justify-between items-center">
            <div class="flex items-center">
              <svg class="w-4 h-4 mr-2 text-green-600 transform group-open:rotate-90 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
              </svg>
              <h3 class="text-base font-medium text-green-900">材料費</h3>
              <% if @material_confirmed %>
                <span class="ml-2 px-2 py-0.5 text-xs bg-green-100 text-green-800 rounded-full">確認済</span>
              <% end %>
            </div>
            <span class="text-base font-bold text-green-900"><%= number_to_currency(@total_material_cost, unit: "¥", precision: 0) %></span>
          </div>
        </summary>
        <div class="divide-y divide-gray-100">
          <% @material_details.each do |detail| %>
            <div class="px-4 py-2 flex justify-between text-sm">
              <span class="text-gray-700"><%= detail[:name] %></span>
              <span class="text-gray-900"><%= number_to_currency(detail[:amount], unit: "¥", precision: 0) %></span>
            </div>
          <% end %>
          <% if @material_details.empty? %>
            <div class="px-4 py-3 text-sm text-gray-500">データなし</div>
          <% end %>
          <div class="px-4 py-2 bg-green-50 flex justify-between items-center">
            <% if @material_confirmed %>
              <%= button_to "確認解除", unconfirm_monthly_cost_path(year: @year, month: @month, cost_type: "material"),
                  method: :delete,
                  class: "text-sm text-gray-500 hover:text-red-600 cursor-pointer" %>
            <% else %>
              <%= button_to "確認済みにする", confirm_monthly_cost_path(year: @year, month: @month, cost_type: "material"),
                  method: :post,
                  class: "text-sm text-green-700 hover:underline cursor-pointer" %>
            <% end %>
          </div>
        </div>
      </details>

      <%# 経費 %>
      <details class="bg-white shadow rounded-lg overflow-hidden group">
        <summary class="px-4 py-3 border-b border-gray-200 bg-gray-100 cursor-pointer list-none">
          <div class="flex justify-between items-center">
            <div class="flex items-center">
              <svg class="w-4 h-4 mr-2 text-gray-600 transform group-open:rotate-90 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
              </svg>
              <h3 class="text-base font-medium text-gray-900">経費</h3>
              <% if @expense_confirmed %>
                <span class="ml-2 px-2 py-0.5 text-xs bg-green-100 text-green-800 rounded-full">確認済</span>
              <% end %>
            </div>
            <span class="text-base font-bold text-gray-900"><%= number_to_currency(@total_expense_cost, unit: "¥", precision: 0) %></span>
          </div>
        </summary>
        <div class="divide-y divide-gray-100">
          <% @expense_details.each do |detail| %>
            <div class="px-4 py-2 flex justify-between text-sm">
              <span class="text-gray-700"><%= detail[:name] %></span>
              <span class="text-gray-900"><%= number_to_currency(detail[:amount], unit: "¥", precision: 0) %></span>
            </div>
          <% end %>
          <% if @expense_details.empty? %>
            <div class="px-4 py-3 text-sm text-gray-500">データなし</div>
          <% end %>
          <div class="px-4 py-2 bg-gray-100 flex justify-between items-center">
            <% if @expense_confirmed %>
              <%= button_to "確認解除", unconfirm_monthly_cost_path(year: @year, month: @month, cost_type: "expense"),
                  method: :delete,
                  class: "text-sm text-gray-500 hover:text-red-600 cursor-pointer" %>
            <% else %>
              <%= button_to "確認済みにする", confirm_monthly_cost_path(year: @year, month: @month, cost_type: "expense"),
                  method: :post,
                  class: "text-sm text-gray-700 hover:underline cursor-pointer" %>
            <% end %>
          </div>
        </div>
      </details>

      <%# 固定費 %>
      <details class="bg-white shadow rounded-lg overflow-hidden group">
        <summary class="px-4 py-3 border-b border-gray-200 bg-purple-50 cursor-pointer list-none">
          <div class="flex justify-between items-center">
            <div class="flex items-center">
              <svg class="w-4 h-4 mr-2 text-purple-600 transform group-open:rotate-90 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
              </svg>
              <h3 class="text-base font-medium text-purple-900">固定費</h3>
            </div>
            <span class="text-base font-bold text-purple-900"><%= number_to_currency(@fixed_costs_total, unit: "¥", precision: 0) %></span>
          </div>
        </summary>
        <div class="divide-y divide-gray-100">
          <% @fixed_costs_by_category.each do |category, amount| %>
            <div class="px-4 py-2 flex justify-between text-sm">
              <span class="text-gray-700"><%= MonthlyFixedCost::CATEGORIES[category] || category %></span>
              <span class="text-gray-900"><%= number_to_currency(amount, unit: "¥", precision: 0) %></span>
            </div>
          <% end %>
          <% if @fixed_costs.empty? %>
            <div class="px-4 py-3 text-sm text-gray-500">データなし</div>
          <% end %>
          <div class="px-4 py-2 bg-purple-50">
            <%= link_to "固定費を編集", monthly_fixed_costs_path(year: @year, month: @month),
                class: "text-sm text-purple-700 hover:underline" %>
          </div>
        </div>
      </details>

      <%# 原価合計 %>
      <div class="bg-orange-100 rounded-lg p-4">
        <div class="flex justify-between items-center">
          <span class="text-sm font-medium text-orange-800">原価合計</span>
          <span class="text-xl font-bold text-orange-900"><%= number_to_currency(@total_cost, unit: "¥", precision: 0) %></span>
        </div>
      </div>
    </div>
  </div>

</div>
