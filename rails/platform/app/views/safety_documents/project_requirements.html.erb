<div class="max-w-2xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
  <div class="px-4 py-6 sm:px-0">
    <!-- 戻るリンク -->
    <div class="mb-4">
      <%= link_to safety_documents_path(view: "projects"), class: "text-blue-600 hover:text-blue-800 text-sm" do %>
        ← 安全書類一覧に戻る
      <% end %>
    </div>

    <div class="bg-white shadow rounded-lg overflow-hidden">
      <!-- ヘッダー -->
      <div class="px-6 py-4 border-b bg-gray-50">
        <h2 class="text-lg font-bold text-gray-900"><%= @project.name %></h2>
        <p class="text-sm text-gray-600 mt-1">この案件に必要な安全書類を選択してください</p>
      </div>

      <!-- クイック操作 -->
      <div class="px-6 py-3 border-b bg-blue-50 flex flex-wrap items-center gap-2">
        <%= button_to set_all_project_requirements_safety_documents_path(project_id: @project.id),
            method: :post,
            data: { turbo_confirm: "全ての書類を必要に設定しますか？" },
            class: "text-xs px-3 py-1 bg-blue-600 text-white rounded hover:bg-blue-700" do %>
          全て選択
        <% end %>
        <%= button_to clear_project_requirements_safety_documents_path(project_id: @project.id),
            method: :delete,
            data: { turbo_confirm: "設定をクリアしますか？（共通設定が適用されます）" },
            class: "text-xs px-3 py-1 bg-gray-200 text-gray-700 rounded hover:bg-gray-300" do %>
          クリア（共通設定を使用）
        <% end %>
      </div>

      <!-- チェックボックスフォーム -->
      <%= form_with url: project_requirements_safety_documents_path(project_id: @project.id), method: :patch, local: true do |f| %>
        <div class="divide-y divide-gray-200">
          <% @document_types.each do |doc_type| %>
            <label class="flex items-start px-6 py-4 hover:bg-gray-50 cursor-pointer">
              <div class="flex items-center h-5">
                <%= check_box_tag "safety_document_type_ids[]", doc_type.id,
                    @selected_ids.include?(doc_type.id),
                    class: "h-4 w-4 text-blue-600 border-gray-300 rounded focus:ring-blue-500" %>
              </div>
              <div class="ml-3">
                <span class="text-sm font-medium text-gray-900"><%= doc_type.name %></span>
                <% if doc_type.description.present? %>
                  <p class="text-xs text-gray-500 mt-0.5"><%= doc_type.description %></p>
                <% end %>
              </div>
            </label>
          <% end %>
        </div>

        <% if @document_types.empty? %>
          <div class="px-6 py-8 text-center text-gray-500">
            書類種類が登録されていません。<br>
            <%= link_to "書類種類を登録", safety_document_types_path, class: "text-blue-600 hover:text-blue-800" %>してください。
          </div>
        <% end %>

        <!-- 送信ボタン -->
        <div class="px-6 py-4 border-t bg-gray-50 flex justify-end gap-3">
          <%= link_to "キャンセル", safety_documents_path(view: "projects"),
              class: "px-4 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-300 rounded-md hover:bg-gray-50" %>
          <%= f.submit "保存", class: "px-4 py-2 text-sm font-medium text-white bg-blue-600 border border-transparent rounded-md hover:bg-blue-700 cursor-pointer" %>
        </div>
      <% end %>
    </div>

    <!-- 説明 -->
    <div class="mt-6 bg-yellow-50 border border-yellow-200 rounded-lg p-4">
      <div class="flex">
        <svg class="h-5 w-5 text-yellow-400 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
          <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
        </svg>
        <div class="ml-3">
          <h3 class="text-sm font-medium text-yellow-800">設定について</h3>
          <div class="mt-1 text-sm text-yellow-700">
            <ul class="list-disc ml-4 space-y-1">
              <li>チェックを付けた書類のみが、この案件の提出チェック対象になります</li>
              <li>何も選択しない場合、共通設定（全ての有効な書類種類）が適用されます</li>
              <li>「クリア」ボタンで共通設定に戻すことができます</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
