<div class="max-w-4xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
  <div class="flex justify-between items-center mb-6">
    <div>
      <h1 class="text-2xl font-bold text-gray-900"><%= @year %>年<%= @month %>月 確定給与</h1>
      <p class="text-sm text-gray-500">社労士計算後の給与を入力（正社員のみ）</p>
    </div>
    <div class="flex space-x-2">
      <%= link_to "月次損益に戻る", monthly_profit_loss_path(year: @year, month: @month),
          class: "inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50" %>
    </div>
  </div>

  <%# 月移動ナビゲーション %>
  <div class="flex items-center justify-between mb-4 bg-gray-50 rounded-lg p-3">
    <% prev_date = Date.new(@year, @month, 1).prev_month %>
    <% next_date = Date.new(@year, @month, 1).next_month %>
    <%= link_to monthly_salaries_path(year: prev_date.year, month: prev_date.month),
        class: "inline-flex items-center text-sm text-gray-600 hover:text-gray-900" do %>
      <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
      </svg>
      <%= prev_date.month %>月
    <% end %>
    <span class="font-medium text-gray-900"><%= @year %>年<%= @month %>月</span>
    <%= link_to monthly_salaries_path(year: next_date.year, month: next_date.month),
        class: "inline-flex items-center text-sm text-gray-600 hover:text-gray-900" do %>
      <%= next_date.month %>月
      <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
      </svg>
    <% end %>
  </div>

  <%# ステータス表示 %>
  <% if @is_confirmed %>
    <div class="mb-4 bg-green-50 border border-green-200 rounded-lg p-3">
      <div class="flex items-center">
        <svg class="w-5 h-5 text-green-600 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
        </svg>
        <span class="text-sm text-green-800">確定給与が入力済みです（合計: <%= number_to_currency(@total, unit: "¥", precision: 0) %>）</span>
      </div>
    </div>
  <% else %>
    <div class="mb-4 bg-yellow-50 border border-yellow-200 rounded-lg p-3">
      <div class="flex items-center">
        <svg class="w-5 h-5 text-yellow-600 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
        </svg>
        <span class="text-sm text-yellow-800">確定給与が未入力です。月次損益では現場台帳の概算値が使用されます。</span>
      </div>
    </div>
  <% end %>

  <%= form_with url: bulk_update_monthly_salaries_path(year: @year, month: @month), method: :patch, class: "space-y-4" do |f| %>
    <div class="bg-white shadow rounded-lg overflow-hidden">
      <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">社員名</th>
            <th class="px-4 py-3 text-right text-xs font-medium text-gray-500 uppercase">確定給与</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">備考</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-200">
          <% @regular_employees.each do |employee| %>
            <% salary = @salaries[employee.id] %>
            <tr class="hover:bg-gray-50">
              <td class="px-4 py-3">
                <div class="text-sm font-medium text-gray-900"><%= employee.name %></div>
                <div class="text-xs text-gray-500"><%= employee.code %></div>
              </td>
              <td class="px-4 py-3">
                <div class="relative rounded-md shadow-sm">
                  <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                    <span class="text-gray-500 sm:text-sm">¥</span>
                  </div>
                  <%= text_field_tag "salaries[#{employee.id}][total_amount]",
                      salary&.total_amount&.to_i,
                      class: "block w-full pl-8 pr-3 py-2 text-right rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm",
                      placeholder: "0",
                      inputmode: "numeric" %>
                </div>
              </td>
              <td class="px-4 py-3">
                <%= text_field_tag "salaries[#{employee.id}][note]",
                    salary&.note,
                    class: "block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm",
                    placeholder: "備考" %>
              </td>
            </tr>
          <% end %>
        </tbody>
        <tfoot class="bg-gray-100">
          <tr>
            <td class="px-4 py-3 text-sm font-bold text-gray-900">合計</td>
            <td class="px-4 py-3 text-right text-lg font-bold text-gray-900">
              <%= number_to_currency(@total, unit: "¥", precision: 0) %>
            </td>
            <td class="px-4 py-3"></td>
          </tr>
        </tfoot>
      </table>
    </div>

    <div class="flex justify-end space-x-3">
      <%= link_to "キャンセル", monthly_profit_loss_path(year: @year, month: @month),
          class: "inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50" %>
      <button type="submit" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 cursor-pointer">
        保存
      </button>
    </div>
  <% end %>

  <div class="mt-6 bg-blue-50 border border-blue-200 rounded-lg p-4">
    <h4 class="text-sm font-medium text-blue-800 mb-2">入力について</h4>
    <ul class="text-sm text-blue-700 space-y-1">
      <li>社労士から受け取った確定給与（総支給額）を入力してください</li>
      <li>0円または空欄の場合、その社員の給与は保存されません</li>
      <li>確定給与が入力されると、月次損益の労務費に反映されます</li>
      <li>未入力の場合は、現場台帳の概算値（単価×人工）が使用されます</li>
    </ul>
  </div>
</div>
